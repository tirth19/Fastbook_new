<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SignupActivity">
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="100dp"
        android:src="@drawable/fastbook_logo"
        android:layout_centerHorizontal="true"
        android:id="@+id/logo"
        android:background="@drawable/round_image"/>

    <com.google.android.gms.common.SignInButton
        android:id="@+id/btn_google_signin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="348dp"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Connect Google Account to Signup"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/btn_google_signin"/>

    <TextView
        android:id="@+id/textView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="108dp"
        android:text="FastBook"
        android:textColor="@color/black"
        android:textSize="25sp"
        android:layout_below="@id/logo"
        android:layout_centerHorizontal="true" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Click here to sign in if you already have an account"
        android:layout_below="@id/textView"
        android:layout_centerHorizontal="true"
        android:id="@+id/link_to_sign_in_activity"/>
</RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>